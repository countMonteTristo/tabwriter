No whitespace characters act as tokens - optional for human readability

Parsing rationale (base coade) - all non-coding chars ignored; no errors thrown

Perform Regex (?) to parse string


CODING STRINGS:

1. 	Notes - one letter from range [E, a, d, g, b, e] plus ONE or TWO digits 
	gves string + fret

2.	{} curly braces with containing ONE or TWO digits indicates notes per bar

3.	[] square braces containing Notes (see 1.) indicate chord i.e.  or more 
	notes played at same time.

4.	() contains  title or any other descriptors req'd that can span at most 3 lines at
	top of Tab document. All chars incl. whitespace within this ()
   	
 
